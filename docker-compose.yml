# Wskazuje z jakiej bazy korzystamy, mówi o użytkowniku i nazwie bazy danych

services:
  db:
    image: postgres:16
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 123
      POSTGRES_DB: nestjs
